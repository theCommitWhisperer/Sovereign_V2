-- Shown to the player upon losing a match.
local React = require(game.ReplicatedStorage.Packages.react)
local DefeatScreen = React.Component:extend("DefeatScreen")
function DefeatScreen:render() return nil end
return DefeatScreen
