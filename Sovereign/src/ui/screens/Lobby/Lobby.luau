-- The game lobby screen where players prepare for a match.
local React = require(game.ReplicatedStorage.Packages.react)
local Lobby = React.Component:extend("Lobby")
function Lobby:render() return nil end
return Lobby
